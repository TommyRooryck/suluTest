{% extends "base.html.twig" %}

{% block content %}

    <div class="container-fluid"
        <div class="row">
            <div class="col-12 text-center">
                    <h1>{{ content.title }}</h1>
            </div>
        </div>
    </div>
{% endblock %}
